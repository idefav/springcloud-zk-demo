server:
  port: 8081


spring:
  cloud:
    discovery:
      enabled: true
    zookeeper:
      connect-string: 192.168.0.111:32643
      discovery:
        enabled: true
        root: /services
        instance-host: ${spring.cloud.client.ip-address}
  application:
    name: user-service